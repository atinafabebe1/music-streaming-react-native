import { StyleSheet } from 'react-native';

const TextStyle = StyleSheet.create({
  heading: {
    fontSize: 24,
    fontWeight: 'bold',
    marginBottom: 16
  },
  text: {
    fontSize: 16,
    color: '#333'
  }
});

export default TextStyle;
